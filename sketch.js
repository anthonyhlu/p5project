var cow, chicken, pig, penguin
var nextStepButton
var bkcolor


function setup() {
  createCanvas(750, 500);
  bk = 'rgba(0,255,0, 0.25)';
  text("draw like ant", 100, 100)
  textSize(100)
  nextStepButton = createButton("next step")
  nextStepButton.position(750/3, 500);
  nextStepButton.size(300)
}

function draw() {

  

}

